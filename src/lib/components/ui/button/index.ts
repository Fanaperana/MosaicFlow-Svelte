import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonClass,
} from "./button.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	buttonClass,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
