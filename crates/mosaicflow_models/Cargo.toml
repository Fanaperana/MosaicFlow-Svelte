[package]
name = "mosaicflow_models"
version = "0.1.0"
description = "MosaicFlow data models - vault, canvas, workspace, state, history, config"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
mosaicflow_core = { path = "../mosaicflow_core" }
